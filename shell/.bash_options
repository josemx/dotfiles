#!/bin/bash

# autocorrect typos in path names
shopt -s cdspell

# recalculate LINES and COLUMNS after command and window resize
shopt -s checkwinsize

# append to the bash history file
shopt -s histappend